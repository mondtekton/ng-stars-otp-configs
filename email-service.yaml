# ============ SERVER CONFIGURATION =============
server:
  port: 8082

# ============== Eureka Discovery configuration ==================== #
eureka:
  client:
    serverUrl:
      defaultZone: http://localhost:8761/eureka/
