# ============== Application configuration ==================== #
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: katohdavid@gmail.com
    password: fxzyuynzfnfvqoqo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  endpoints:
    web:
      exposure:
        include: ["prometheus"]

email:
  sender: katohdavid@gmail.com

# ============== Eureka Discovery configuration ==================== #
eureka:
  client:
    fetch-registry: true
    serviceUrl:
      defaultZone: http://${DOCKER_HOST:localhost}:9293/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true

env: Dev Environment. All configuration here are meant for development.
