spring:
  datasource: # Database configuration #
    url: jdbc:mysql://localhost:3306/files_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 38NG/db@tro38
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

file:
  env: File service in development environment.
  upload-dir: ${user.home}/FileStorage
