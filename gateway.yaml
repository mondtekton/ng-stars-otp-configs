spring:
  cloud:
    gateway:
      routes:
        - id: otp
          uri: lb://otp
          predicates:
            - Path=/otp/**
        - id: email
          uri: lb://email
          predicates:
            - Path=/email/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9293/eureka/
